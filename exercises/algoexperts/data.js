function sayHello(character) {
   // Extract data
   const { name, message } = character

   // Here I'm going to talk about
   return {
      name,
      message,
   }
}

console.log('This is sparta!')
let hello = 'Hi there dsds dssdd'

console.log(hello)

// Nala greetings
sayHello({
   name: 'Nala',
   message: 'Meouww',
})

// More greetings
const churrita = {
   name: 'Churrita ♥️',
   message: 'Hello Hello :D finalmente',
}

const l = sayHello(churrita)
l

// Vars
const name = 'Edwin'
const age = 34
const sex = 'somethings'
